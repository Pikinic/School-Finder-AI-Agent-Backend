/** @type {import('prettier').Config} */
const config = {
  arrowParens: 'always',
  endOfLine: 'lf',
  printWidth: 80,
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'all',
}

export default config
